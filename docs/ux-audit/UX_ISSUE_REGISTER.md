# UX Issue Register

Severity: `P0` blocks task completion/security/data-loss/nav-trap · `P1` major broken journey · `P2` important usability/responsive/a11y/consistency · `P3` polish

| Issue ID | Screen ID(s) | Title | Category | Severity | Status |
|---|---|---|---|---|---|
| ISS-0001 | PUB-12/13/14 | 8 of 11 CLAUDE.md-required legal/policy pages missing (Cookie, Cancellation, Listing, Advertising, Verification, Payment, Disclaimer, Grievance) | content/legal | P1 | FIX_REQUIRED |
| ISS-0002 | ALL | No shared modal/drawer/sheet primitive — only 5 dedicated components exist for 98 routes; overlay UX likely inconsistent per-screen | design-system | P2 | DISCOVERED (needs per-module confirmation before fix) |
| ISS-0003 | ALL | No test script in package.json — batch verification cannot include a real automated test PASS | tooling | P2 | BLOCKED_WITH_REASON (no test infra; requires a decision on whether to add one — out of scope to invent framework choice unilaterally) |
| ISS-0004 | ALL | No central middleware — every route's auth/role guard must be verified individually; higher risk of an unguarded route | security/architecture | P1 | **VERIFIED — no defect found.** Grepped all 98 `page.tsx`/`route.ts` files for `requireRole`/`requireAuth` usage; every route not covered by the grep was individually inspected and is legitimately public or token-gated: `admin/staff/invites` (redirect to `/admin/staff`, not a real screen), `dashboard/page.tsx` (checks `getCurrentProfile()` and redirects to `/login` if absent), `admin/invite` + `team/invite` (pre-auth token-based invite acceptance by design, not role-gated). No unguarded protected route found. |

## Batch 1 findings (P0/P1 static sweep, 2026-07-14)

Static/grep-level checks run across the full `src/app` + `src/components` tree (98 routes + shared components):

| Check | Result | Evidence |
|---|---|---|
| `href="#"` anywhere in codebase | **PASS — none found** | Grep across `src`, 0 matches |
| Empty/no-op `onClick={() => {}}` handlers | **PASS — none found** | Tight-pattern grep across `src`, 0 matches (an earlier loose regex falsely flagged 59 files — all were real handlers with bodies) |
| Missing auth guard on protected routes | **PASS — none found** | See ISS-0004 above |
| Disabled/"coming soon" action cards rendered as fake-clickable | **PASS — correct pattern** | `ActionCard.tsx` only renders the `<a href>` "Open →" affordance when `status === "active"`; `coming_soon`/`setup_required` cards show a status chip instead and are never wrapped in a link — matches CLAUDE.md §2.7 "no fake functionality" |
| Sample of static internal `href="/…"` literals resolve to real registered routes | **PASS on sample** | 42 literal-prefix hrefs extracted, all matched entries in [COMPLETE_SCREEN_REGISTRY.md](COMPLETE_SCREEN_REGISTRY.md); this check is **not exhaustive** — most `href`s in the codebase are built from template literals (`` `/dashboard/${role}/properties/${id}` ``) which a static grep cannot fully validate. A dynamic/live check is still required per route. |

**Not yet done (real gap, not claimed complete):** the checks above are static-analysis only. They cannot catch runtime dead ends, broken Back/Close/Cancel behavior, unsaved-change data loss, mobile navigation traps, or role/permission leaks that only show up when a route is actually opened, clicked through, and observed in a browser across breakpoints. That requires the live per-route walkthrough described in §33 of the audit brief, which has only been done so far for 5 routes total (OWN-01, BRK-01, BLD-01 dashboard homes + the 2 login journeys) — see [NAVIGATION_BEHAVIOR_MATRIX.md](NAVIGATION_BEHAVIOR_MATRIX.md) and [USER_JOURNEY_MATRIX.md](USER_JOURNEY_MATRIX.md).

Full per-screen issue findings will be appended here as each batch runs its live-verification step.

## Batch 1B — Owner role live audit (2026-07-14)

| Issue ID | Screen ID(s) | Title | Category | Severity | Status |
|---|---|---|---|---|---|
| ISS-0005 | PUB-03/PUB-04 (reached via OWN-02 "View") | Owner (and Builder) viewing their **own** live property/project via the dashboard "View" link saw the full buyer-facing "Call" / "Send an enquiry" CTA bar. Server already rejected self-enquiry (`VALIDATION_ERROR`, "You cannot inquire about your own listing") and self-reveal (`SELF_ACTION`), but the UI let the poster open the whole enquiry form and submit before hitting that rejection — a dead-end per rule 2.7 ("fake functionality": a control that can never succeed must be disabled with an honest reason, not clickable-then-error). | interaction/dead-end | P1 | **FIXED + VERIFIED.** Added `isOwnListing` prop to `DetailCTABar` (`src/components/detail/DetailCTABar.tsx`); when true, both the desktop sidebar and mobile sticky bar now show "This is your listing — Call/Enquire show to other visitors only." instead of the Reveal/Call/Enquire controls — same honest-disabled pattern already used for `isRestrictedRole`. Wired via `owner_profile_id`/`builder_profile_id` comparison in `src/app/property/[slug]/page.tsx` and `src/app/project/[slug]/page.tsx` (both already selected these columns, just weren't using them for this check). Verified live logged in as Owner test account at 687px and 1440px — correct honest message renders both places, no regression to the buyer-facing flow (guest/other-owner paths untouched, all guarded by `isOwnListing = false` default). typecheck PASS, lint PASS. |
| ISS-0006 | OWN-03 (`/dashboard/owner/properties/new`) | Initially suspected: clicking "Continue" on the draft-resume card at the wizard entry point appeared to do nothing (URL never changed after repeated coordinate-based clicks). | interaction | ~~P0~~ **FALSE POSITIVE — corrected, not a real defect.** Root-caused by isolating the click from the test harness: hooked `history.pushState` and called the real DOM button's `.click()` directly (bypassing the browser-automation tool's coordinate-based synthetic click) against the **original, unmodified** `DraftResumeCard.tsx` — navigation fired correctly and the URL updated to `?draft=<id>` both before and after a defensive edit. The apparent "dead button" was the automation tool's coordinate click missing the element (button position in the accessibility-tree coordinate vs. rendered pixel position drifted, likely from a layout shift), not application code. **Correction applied to audit methodology:** critical-action verification will cross-check with a direct DOM click and URL/screenshot diff before a click is reported as a dead button, to avoid further false positives. A small defensive hardening was still kept (`DraftResumeCard.tsx` now resolves its default `Continue`/`Start New` targets via `usePathname()` instead of a bare `?query` string) since it's strictly more robust and was verified not to change behavior — typecheck PASS, lint PASS, live-reverified both before/after. |
| ISS-0007 | SHR-03 (`/dashboard/leads/[id]`) | Lead Detail screen (shared across all roles) had no mobile Back button — `DashboardShellV2` already supports a `mobileBackHref` prop specifically for this (CLAUDE.md §3A rule 5: "every mobile inner screen has a contextual header with a back button"), but the page never passed it, so on mobile the only way back to the Leads list was the bottom-nav "Leads" tab (a fresh list reload, not a true Back). | navigation/dead-end | P1 | **FIXED + VERIFIED.** Added `mobileBackHref={activeHref}` (`/dashboard/{role}/leads`) to the `DashboardShellV2` call in `src/app/dashboard/leads/[id]/page.tsx`. Verified live as Owner at 375px: back header now renders "← Lead Detail", clicking it returns correctly to `/dashboard/owner/leads`. typecheck PASS, lint PASS. Same component is reused for Broker/Builder — fix applies to all roles since the route and its guard are role-agnostic. |
| ISS-0008 (supporting evidence, no new fix required) | Dev seed data | `/dashboard/owner/leads` shows a pre-existing self-lead: "HOUSE FOR SALE IN RAJOKOT — With **My**" — an owner-authored lead on the owner's own listing, evidently created before ISS-0005's fix existed. Confirms ISS-0005 was a real, already-manifested defect in this environment, not just a theoretical one. | data-integrity | — | **No action taken** — this is existing dev/seed data, not a code defect (ISS-0005's fix already prevents new self-leads going forward). Consistent with this project's existing practice of flagging dev fixtures for cleanup before production (see memory: Site Visits Dev Fixture, Gallery Dev Fixture) — recorded here for traceability, not a new cleanup task owned by this audit. |
| ISS-0009 | Systemic — 21 routes across Owner/Broker/Builder/shared | Same root cause as ISS-0007 found to be **codebase-wide, not a one-off**: a full grep sweep of every `page.tsx` using `DashboardShellV2` found 35 pages missing `mobileBackHref` (or the root-screen `mobileDrawerNav`/`hideTopbarOnMobile` alternative) entirely — meaning no mobile Back control at all on: Owner/Broker/Builder Billing, Notifications, Saved, Verification, Public Profile, Properties/Requirements/Projects/Ads lists, Proposals, Leads, Agents, Requirements-new/edit, Ads-new/edit, Construction, Units, plus the shared `Messages`, `Billing/GST`, and `Profile` pages. | navigation/dead-end | P1 | **FIXED + VERIFIED (systemic).** Added `mobileBackHref` to all 32 genuinely non-root pages (pointing to the dashboard home for top-level modules, or to the immediate parent list for `new`/`edit`/sub-detail pages, e.g. `ads/[id]/edit` → `/dashboard/builder/ads`); role-agnostic shared pages (`messages`, `billing/gst`, `profile`) use `` `/dashboard/${profile.public_role}` `` dynamically. The remaining 3 (owner/broker/builder dashboard **home** pages) correctly stay without it — those are root bottom-nav destinations. `npm run build` passed for all 98 routes after the sweep; typecheck PASS; lint PASS. Spot-verified live: Owner Saved Items now shows "← Saved Items" and returns correctly. |
| ISS-0010 | BRK-01, BLD-01 | While fixing ISS-0009, found Broker's and Builder's dashboard **home** pages had neither `mobileBackHref` (correct, they're roots) **nor** `mobileDrawerNav` — unlike Owner's home, which uses `mobileDrawerNav={getOwnerDrawerNav(...)}` for a full hamburger-menu nav on mobile. Result: on mobile, Broker/Builder users could only reach the 5 bottom-nav tabs and had **no way to reach** Analytics, Billing, Verification, Public Profile, Notifications, or Settings — a real navigation dead-end for two of three roles, not just a cosmetic gap. | navigation/dead-end | P1 | **FIXED + VERIFIED.** Added `mobileDrawerNav={getBrokerNav("/dashboard/broker")}` / `mobileDrawerNav={getBuilderNav("/dashboard/builder")}` to their respective home pages — reused the existing sidebar nav functions directly (they're already a complete superset, unlike Owner's which needed a separate `getOwnerDrawerNav`), so no nav-data duplication was introduced. Verified live as Broker at 375px: header now shows a hamburger icon; opening it lists all 14 modules (Overview → Support) and closes correctly. typecheck PASS, lint PASS, build PASS. |
| ISS-0011 | OWN-16 (`/dashboard/owner/recently-viewed`) | Pre-existing inconsistency (not introduced this session): this page passed **both** `mobileBackHref` and `mobileDrawerNav` to `DashboardShellV2`; the component's own logic gives `mobileDrawerNav` priority, so the page silently showed a full hamburger-menu header instead of the simple "← Recently Viewed" back header every other non-root inner page uses (Saved, Leads, Site Visits, etc. all show Back only). Not a dead-end (the hamburger is functional) but breaks the established, consistent pattern. Broker's equivalent page did **not** have this bug (only `mobileBackHref`), confirming it's an isolated copy-paste artifact on the Owner page only. | consistency | P2 | **FIXED + VERIFIED.** Removed the extraneous `mobileDrawerNav={getOwnerDrawerNav(...)}` prop and its now-unused import from `src/app/dashboard/owner/recently-viewed/page.tsx`, leaving only `mobileBackHref="/dashboard/owner"`. Verified live at 375px: page now shows "← Recently Viewed" consistent with every other inner page. typecheck PASS, lint PASS. |
| ISS-0012 | OWN-15/BRK-notifications/BLD-notifications (`/dashboard/{role}/notifications`) | Confirmed via direct DOM click (not a coordinate-tool false positive — cross-checked per the ISS-0006 methodology correction) that clicking a notification only navigated for `target_type: "lead"` or `"message_thread"`. Notifications of type `site_visit` (e.g. "Site visit rescheduled") and `proposal` (e.g. "New proposal received") marked themselves read but **never navigated anywhere** — a real dead click, violating the explicit rule "Notification click must navigate to related page." The header bell dropdown (`NotificationBell.tsx`) has the same gap for `site_visit` (partial: it does handle `proposal` → `/dashboard/messages`, just not `site_visit`). | interaction/dead-end | P1 | **FIXED + VERIFIED for the full Notifications page.** `NotificationListClient` now takes a `role` prop (passed by all 3 role-specific `notifications/page.tsx` server pages, which already know the role) and routes `site_visit` → `/dashboard/{role}/site-visits`, `proposal` → `/dashboard/broker/proposals` (broker) or `/dashboard/{role}/requirements` (owner/builder), and any other/future type → `/dashboard/{role}` as a safe generic fallback — no notification type can dead-end anymore on this screen. Verified live as Owner: clicking "Site visit rescheduled" now navigates to `/dashboard/owner/site-visits`. typecheck PASS, lint PASS. **Deferred, smaller scope:** the header bell dropdown (`NotificationBell.tsx`) still has the `site_visit` gap — fixing it requires threading a role prop through `DashboardTopbar`/`DashboardMobileHeader`/`DashboardShellV2` (4 components), a larger shared-component change out of scope for this pass; logged for a follow-up batch rather than left silently unfixed. |

## Batch 1C — Broker role live audit (2026-07-14)

| Issue ID | Screen ID(s) | Title | Category | Severity | Status |
|---|---|---|---|---|---|
| ISS-0013 | Shared component — affects 6 list pages (BRK-05 `requirements`, BRK-02 `properties`, OWN-05, OWN-02, BLD-projects, + any future usage) | `DashboardPageHeader`'s title `<h1 className="truncate">` shared one flex row with a wide action button (`+ Post Requirement` etc.); at 375px this left too little room and the **static page heading itself** truncated mid-word ("My Requirem…") — not a long dynamic value, a fixed 16-character English string that should never need to shrink. Violates CLAUDE.md §15 ("no tiny fonts... no cramped/broken layout") at the 320-390px required breakpoints. | responsive | P2 | **FIXED + VERIFIED.** `DashboardPageHeader.tsx`: layout is now `flex-col` (title full-width on its own row, button below) below `sm`, reverting to the original single-row `flex-row items-center justify-between` at `sm:` and up. Verified live at 375px ("My Requirements" now shows in full, button below) and 1024px (unchanged side-by-side layout, no regression). Fixes all 6 pages that use this shared component in one change. typecheck PASS, lint PASS. |

## Batch 1D — Builder role live audit (2026-07-14)

| Issue ID | Screen ID(s) | Title | Category | Severity | Status |
|---|---|---|---|---|---|
| ISS-0014 | BLD-04 (`/dashboard/builder/projects/[id]/edit`) | **Severe navigation trap, confirmed live.** `ProjectForm` (used for the Builder project create/edit wizard) had **no internal mobile header at all** — unlike its sibling `PropertyForm`, which renders its own Back/title/Save-Draft bar for every step. Combined with `WizardShell` deliberately hiding the dashboard's own topbar *and* bottom nav on mobile (by design, so the form provides its own chrome), this meant a Builder editing a project on mobile had **zero way to leave the screen** except the browser Back button — no title, no Back link, no bottom nav, nothing. Directly caught live at 375px, not a coordinate-tool artifact (confirmed via `window.location.href` + full accessibility-tree dump showing no back-capable link/button anywhere on the page). | navigation/dead-end | **P0** | **FIXED + VERIFIED.** Added the same internal mobile header pattern `PropertyForm` already uses to `ProjectForm.tsx` (Back button on step>1 / Back-to-list link on step 1, title reflecting create vs edit mode). Verified live at 375px on both `/dashboard/builder/projects/new` and `.../[id]/edit` — correct single header renders, Back link confirmed to point to `/dashboard/builder/projects`. typecheck PASS, lint PASS. |
| ISS-0015 | BLD-03 (`/dashboard/builder/projects/new`) | Fixing ISS-0014 initially caused a **new duplicate-header regression** on the create flow: `projects/new/page.tsx` used plain `DashboardShellV2` (with its own working `mobileBackHref`, already correct) while `projects/[id]/edit/page.tsx` used `WizardShell` — so adding `ProjectForm`'s internal header fixed Edit but produced two stacked "← Post a Project" headers on New. Caught immediately via live screenshot before shipping, not left in. | regression (self-caught) | P1 | **FIXED + VERIFIED.** Reconciled `projects/new/page.tsx` to use `WizardShell` (matching `edit` and matching the Property wizard's established pattern) instead of `DashboardShellV2`, removing the now-redundant `mobileBackHref` and gating its desktop-only descriptive `<h1>` block behind `hidden lg:block` (previously `hidden sm:block` on the Edit page too, which would have shown a duplicate header on tablet width 640-1023px — fixed on both pages for consistency with Property's exact breakpoint). Verified live at 375px (single header, no duplication) and 1024px (full sidebar shell, correct heading, no regression) on both routes. typecheck PASS, lint PASS. |
| ISS-0016 | BLD-07 (`/dashboard/builder/requirements`) | "Matching Requirements" showed a hardcoded empty-state message "Publish a project first…" **even when the builder already has a published project** (this test account does — "Test Grand Vista Towers", Published) — the real reason for zero matches is no requirement currently matches that project's city/category, not "you haven't published anything," which is misleading and could make a builder think their listing didn't actually go live. | content-accuracy | P2 | **FIXED + VERIFIED.** `getMatchingRequirements()` (`src/lib/actions/proposals.ts`) now returns a `hasPublishedProjects` flag (derived from the same query that already computed it, just wasn't exposed); `builder/requirements/page.tsx` picks between "No matching requirements yet…" (has projects, zero matches) and "Publish a project first…" (genuinely zero published projects) accordingly. Verified live: this builder account (has 1 published project, 0 matches) now correctly shows "No matching requirements yet". typecheck PASS, lint PASS. |
| ISS-0017 | BLD-09 (`/dashboard/builder/agents`) | The team-member "⋮" actions dropdown (`TeamClient.tsx`'s `MemberMenu`) was positioned `absolute right-0` relative to a narrow wrapper around just the button. At 375px, `flex-wrap` on the row caused the status-chip+menu-button group to wrap onto its own line starting from the card's **left** edge (not the right, despite `justify-between`) — so the 160px-wide `right-0`-anchored dropdown extended left past the viewport edge, clipping "Activate"/"Suspend"/"Remove" to unreadable fragments ("...ctivate", "...move"). Confirmed live via direct DOM click + screenshot, not a coordinate-tool artifact. | responsive | P2 | **FIXED + VERIFIED.** Changed the dropdown's anchor from `right-0` to `left-0` — the card is always comfortably wider than the 160px menu regardless of where the trigger button ends up (left-wrapped on mobile, or genuinely right-aligned on wide desktop), so opening rightward from the button is safe at every width. Verified live at 375px: "Activate / Suspend / Remove" now fully readable inside the viewport. typecheck PASS, lint PASS. |
| ISS-0018 | BLD-15 (`/dashboard/builder/billing`) | Billing page showed "Agents 0 / 1" usage even though this builder account has 1 real invited team member (confirmed live on `/dashboard/builder/agents`). Traced to source: `incrementUsage()` for the `agents`/`agents_limit` feature key is **never called anywhere in `src/lib/actions/team.ts`** (the real agent-invite action) — unlike `src/lib/actions/projects.ts:284`, which correctly calls `incrementUsage` on submit. So this counter can never reflect reality for any builder, no matter how many agents they invite through the real UI, and (if/when limits are later enforced) a builder could invite unlimited agents regardless of their plan's stated cap. The "Project posts 0/1" / "Active listings 0/1" showing 0 despite 2 real projects existing is more likely explained by those 2 projects being dev-seeded directly (bypassing `submitProjectForApproval`, which does correctly increment) rather than a code gap — consistent with this project's existing "My"-self-lead seed-data pattern (see ISS-0008). | billing/data-accuracy | P2 | **NOT FIXED this pass — logged, not silently dropped.** This touches billing/usage-gating logic, which CLAUDE.md flags as sensitive (§38: no unilateral changes to billing/payment-adjacent logic without more certainty). The page already carries an honest disclaimer ("Plan limits are not yet enforced (soft-launch)"), which covers the immediate user-facing risk — no one is being blocked or wrongly charged today. Real fix (add `incrementUsage(profile.id, "builder", "agents")` on successful invite in `team.ts`, matching the `projects.ts:284` pattern) is straightforward but deliberately deferred to a dedicated billing-focused pass rather than folded into a UX audit batch. |
| ISS-0019 | BLD-19 (`/dashboard/builder/public-profile`) | Shows "Not published yet" with copy "Your public microsite goes live automatically once you have at least one published project" — but this builder account **does** have a published project ("Test Grand Vista Towers", confirmed live and correctly listed in the "Projects shown (1)" section on the same screen). `isLive` is computed purely from `published?.public_slug` (`getPublicBuilderLinkByProfileId` → `public_builder_profiles_view.public_slug`), and grepping the whole codebase found **no application code that ever writes `public_slug`** — it must come from the database view/migration layer, which this session did not inspect. Also spot-checked and ruled out as a false alarm: a `Link href="\dashboard\builder\projects"` that *looked* like a Windows-backslash path bug in the file read, but the actually-rendered `<a>` tag correctly had forward slashes (Next.js normalizes it) — confirmed no functional break, fixed the source string anyway for hygiene. | content-accuracy / needs-investigation | P2 | **NOT FIXED — logged as needing DB-layer investigation.** Root cause sits in a Postgres view/migration this session didn't open, not in application code reachable safely within a UX-audit pass. Not a dishonesty violation in the strict sense — the page shows an accurate "Not published yet" state (it truly isn't live) rather than faking success; the only inaccuracy is the explanatory copy claiming "automatically," which may not currently be implemented. Flagged for whoever owns the builder-microsite/RLS-view layer to confirm whether `public_slug` generation is genuinely missing or just absent from this seeded test account. |

## Correction — Batch 1B/1C/1D mobile-header rule was wrong (2026-07-14, user-caught)

| Issue ID | Screen ID(s) | Title | Category | Severity | Status |
|---|---|---|---|---|---|
| ISS-0020 | All Owner/Broker/Builder/shared dashboard routes (~50 pages) | **User-flagged design mistake, not a bug found by audit.** ISS-0009's "every non-home page needs a mobile Back button" rule was categorically wrong. It conflated two different kinds of screens: **root pages** — sidebar/drawer nav destinations like My Properties, Requirements, Leads, Saved, Site Visits, Analytics, Billing, Settings, Verification, Messages, Profile — which are peers of Home and should get the same **hamburger drawer** treatment Home already has; and **child pages** — create/edit wizards, single-entity detail views, sub-resource forms — which genuinely need a **Back** button to their parent. Applying Back-only everywhere meant every sidebar-listed screen lost its hamburger/full-nav access on mobile (regressing behind even the pre-audit baseline for Owner, which originally had it correctly on Home only). Additionally, the bottom nav (Home·Search·Post·Leads·Profile) was showing identically on every screen including child/detail screens where it isn't meaningful mid-task. | navigation/dead-end (architecture) | **P0 (self-corrected on user report)** | **FIXED + VERIFIED across all ~50 pages.** Reclassified every dashboard route against each role's actual `getXNav`/`getXDrawerNav` membership: 38 root pages now use `mobileDrawerNav` (reusing the existing sidebar-nav functions — no new nav-data duplication for Broker/Builder since their nav functions were already supersets; Owner reuses `getOwnerDrawerNav` which already existed for Home); 12 true child pages keep `mobileBackHref` to their correct parent **and** now also set `hideMobileTabBar` (bottom nav hidden on focused child screens, consistent with how `WizardShell` already hid it for property/project wizards). Per-role asymmetry preserved intentionally (e.g. Notifications is root for Broker/Builder, child for Owner, matching each role's real nav list — not a bug). Verified live at 375px, 320px and spot 1024px across Owner/Broker/Builder: root pages show hamburger + full nav (confirmed "My Properties" highlighted correctly in the drawer), child pages show Back + no bottom nav (confirmed on Post Requirement wizard and shared Lead Detail), bottom-nav active-tab correctly reflects the current screen (e.g. "Leads" highlighted only on the Leads page). Full `npm run build` passed (all 98 routes). Recorded as a formal rule in [UX_DECISION_LOG.md](UX_DECISION_LOG.md) so it isn't re-broken in later batches (1E Admin, 1G Public). |

Re-ran the requested responsive checks (text wrapping, text clipping, misaligned items) at 320px and 375px across every page touched in this correction batch (Owner Properties/Requirements draft-resume card, Broker/Builder Requirements+Projects headers post-ISS-0013, Builder Agents dropdown post-ISS-0017, dashboard homes, Messages, shared Lead Detail) — all clean, no new issues found.
